// Пакет templates несёт заготовку — пустую, но настоящую модель IDEF0,
// сделанную в самом Ramus. Генератор наполняет её, а не сочиняет файл с нуля:
// в `.rsf` 55 таблиц, метаданные, служебные квалификаторы и блоб сессии GUI,
// и каждая забытая мелочь обнаружилась бы только при открытии в Ramus.
//
// Пакет заведён здесь, а не рядом с генератором, потому что go:embed не умеет
// подниматься выше своего каталога: заготовка обязана лежать в том же дереве,
// что и встраивающий её код.
package templates

import _ "embed"

// IDEF0 — заготовка пустой модели IDEF0.
//
//go:embed template-idef0.rsf
var IDEF0 []byte
