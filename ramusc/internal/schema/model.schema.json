{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Zdertis420/autoramus/schema/model.schema.json",
  "title": "Модель Ramus",
  "$comment": "TODO: секции links (Р7), classifiers (Р8), raw (Р9), layout (Р2) и типы элементов DFD ещё не описаны — сейчас такие поля отвергаются как незнакомые.",

  "type": "object",
  "additionalProperties": false,
  "required": ["model", "flows", "functions"],
  "properties": {
    "model": {
      "$ref": "#/$defs/name",
      "description": "Имя модели; оно же имя корневой работы контекстной диаграммы."
    },
    "author": { "type": "string" },
    "page": { "type": "string" },
    "flows": {
      "description": "Словарь потоков. Обязателен: имя, не объявленное здесь, — ошибка (Р6).",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/name" }
    },
    "functions": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/function" }
    }
  },

  "$defs": {
    "name": {
      "description": "Имя работы или потока. Оно же идентификатор (Р4). Имя из одних пробелов схема пропускает: нормализация пробелов и проверка пустоты после неё — дело этапа IR, там и сообщение получится внятным.",
      "type": "string",
      "minLength": 1
    },
    "flowList": {
      "type": "array",
      "items": { "$ref": "#/$defs/name" }
    },
    "function": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name"],
      "properties": {
        "name": { "$ref": "#/$defs/name" },
        "of": {
          "$ref": "#/$defs/name",
          "description": "Родительская работа. Иерархия задаётся ссылкой, а не вложенностью (Р3)."
        },
        "in": { "$ref": "#/$defs/flowList" },
        "control": { "$ref": "#/$defs/flowList" },
        "out": { "$ref": "#/$defs/flowList" },
        "mechanism": { "$ref": "#/$defs/flowList" },
        "note": {
          "type": "string",
          "description": "Комментарий; попадает в текстовый атрибут и виден в самом Ramus (Р12)."
        }
      }
    }
  }
}
